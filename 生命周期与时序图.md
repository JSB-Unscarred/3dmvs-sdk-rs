# 生命周期与时序图总览

## 所有权边界

```mermaid
flowchart TD
    Session["进程级 SDK session<br/>Initialize / Finalize"]
    SDK["Sdk<br/>session token"]
    Device["Device<br/>handle、动态状态、callback registration、文件名"]
    Processor["ImageProcessor<br/>图像处理 token"]
    Acquisition["Frame / Receiver&lt;Frame&gt; / CallbackWorker<br/>Rust 采集对象"]
    Processed["Image<br/>owned Rust payload"]

    SDK -->|初始化、访问、shutdown| Session
    Device -->|独立持有使用权；live 时阻止 shutdown| Session
    Processor -->|独立持有访问权；调用时校验 session| Session
    Device -->|复制 payload，按需经 channel 交付| Acquisition
    Processor -->|复制 SDK 处理结果| Processed
```

`Sdk` 与 `ImageProcessor` 为 `Send + Sync`，`ImageProcessor` 与 `Device` 均不借用 `Sdk`。`Sdk` 的 `Drop` 只释放 token，session 保持活动。`Device` 为 `Send + !Sync`：它可以在普通 worker thread 中携带活动采集或文件传输的唯一所有权，同一时刻只允许一个线程调用该设备。`Frame`、`Image`、事件和进度快照均拥有 Rust 数据；采集与图像处理在返回前复制 SDK 返回的主数据及可选的亮度数据、曝光时间戳。`shutdown()` 成功后，同一 session 的其他 token 停止接受 native 操作；缓存的 SDK 版本仍可读取。

## Device 完整状态

```mermaid
stateDiagram-v2
    [*] --> Open: open 成功

    Open --> Measuring: start 成功
    Open --> CallbackMeasuring: start_receiving / start_with_callback 成功
    Open --> Transferring: download_file / upload_file 成功
    Open --> Open: start 或 callback start 失败
    Open --> Faulted: 原生 FileAccess 启动结果异常

    Measuring --> Measuring: get_image / soft_trigger / 配置操作
    CallbackMeasuring --> CallbackMeasuring: callback / soft_trigger / stats
    Transferring --> Transferring: Running / 查询错误 / 本地等待超时

    Measuring --> Open: stop 成功
    CallbackMeasuring --> Open: revoke + drain + stop 成功
    Transferring --> Open: 观察到 Completed

    Measuring --> Faulted: stop 失败
    CallbackMeasuring --> Faulted: revoke + drain 后 stop 失败

    Open --> Closed: close / Drop
    Measuring --> Closed: close / Drop 尝试 Stop 后 Close
    CallbackMeasuring --> Closed: revoke + drain + Stop 后 Close
    Transferring --> Closed: 尝试 Close；失败时保留文件名存储
    Faulted --> Closed: close / Drop；采集 Stop 不确定时重试一次
    Closed --> [*]
```

状态转换只在原生调用成功或结果不确定时发生。输入校验失败保持原状态；pull/callback start 失败保持 `Open`。FileAccess 原生启动失败进入 `Faulted`，因为 SDK 可能已开始异步读取文件名。进度查询失败和本地等待超时保持 `Transferring`，便于重试。

| 状态 | 主要可用操作 |
| --- | --- |
| `Open` | 配置、清 buffer、注册或禁用 exception delivery、启动采集、启动文件传输、close |
| `Measuring` | `get_image`、`soft_trigger`、配置、清 buffer、`stop`、close |
| `CallbackMeasuring` | `soft_trigger`、callback stats、`stop`、close |
| `Transferring` | `file_transfer_progress`、`wait_file_transfer`、close |
| `Faulted` | `disable_exception_delivery`、close 或 Drop |

## 清理顺序

1. 撤销 image 与 exception cookie 准入，并等待已准入的 callback 返回。
2. 活动采集调用 Stop；由 Stop 失败进入的 `Faulted` 在 close/Drop 时再尝试一次。
3. 无论 Stop 结果如何都继续尝试 Close，并汇总显式 `close()` 可观察的错误。
4. Close 成功后释放异步文件名并结束该设备的 session 使用权；Close 失败时故意保留文件名存储，并标记 teardown 状态不确定。
5. 最后一个 `Device` 结束后，`Sdk::shutdown()` 才会 Finalize；live handle 导致的拒绝可在关闭设备后直接重试。

各路径细节：

- [标准生命周期与 pull 采集](时序图/标准生命周期与-pull-采集.md)
- [callback 采集与停止](时序图/callback-采集与停止.md)
- [文件上传与下载](时序图/文件上传与下载.md)
